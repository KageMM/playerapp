#matches.transitions-enabled
  - @matches.each do |match|
    .box.panel.panel-default.col-md-4
      = link_to (image_tag match.avatar.url), match
      .panel-body
        %h2=link_to match.title, match
        %p.player
          =match.player.email
